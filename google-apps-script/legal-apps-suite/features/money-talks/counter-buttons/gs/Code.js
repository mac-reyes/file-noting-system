function justinup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C13");

  range.setValue(range.getValue()+1);
}

function justindown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C13");

  range.setValue(range.getValue()-1);
}

function angeup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C14");

  range.setValue(range.getValue()+1);
}

function angedown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C14");

  range.setValue(range.getValue()-1);
}

function tinaup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C15");

  range.setValue(range.getValue()+1);
}

function tinadown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C15");

  range.setValue(range.getValue()-1);
}

function lilyup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C16");

  range.setValue(range.getValue()+1);
}

function lilydown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C16");

  range.setValue(range.getValue()-1);
}

function jaydenup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C17");

  range.setValue(range.getValue()+1);
}

function jaydendown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C17");

  range.setValue(range.getValue()-1);
}

function justinsocup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C20");

  range.setValue(range.getValue()+1);
}

function justinsocdown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C20");

  range.setValue(range.getValue()-1);
}

function angesocup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C21");

  range.setValue(range.getValue()+1);
}

function angesocdown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C21");

  range.setValue(range.getValue()-1);
}

function tinasocup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C22");

  range.setValue(range.getValue()+1);
}

function tinasocdown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C22");

  range.setValue(range.getValue()-1);
}

function lilysocup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C23");

  range.setValue(range.getValue()+1);
}

function lilysocdown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C23");

  range.setValue(range.getValue()-1);
}

function jaydensocup() {
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C24");

  range.setValue(range.getValue()+1);
}

function jaydensocdown(){
  const sheet = SpreadsheetApp.getActiveSheet();
  const range = sheet.getRange("C24");

  range.setValue(range.getValue()-1);
}
